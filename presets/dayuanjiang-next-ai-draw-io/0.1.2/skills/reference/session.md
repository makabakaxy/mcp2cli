# Session Commands

## start — Start a new diagram session

```bash
mcp2cli next-ai-draw-io session start
```

Opens an embedded server and browser window with draw.io for real-time diagram preview. Must be called before using diagram commands.

Use `mcp2cli next-ai-draw-io session start --help` for full parameter details.
